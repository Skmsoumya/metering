{
  "name": "backend",
  "version": "1.0.0",
  "description": "metering backend",
  "main": "bin/www",
  "scripts": {
    "test": "jest --setupFiles dotenv/config --verbose",
    "start": "node ./bin/www",
    "lint": "eslint .",
    "dev_run": "nodemon --exec \"npm run lint && npm run test && node --require dotenv/config ./bin/www dotenv_config_path=.env\""
  },
  "author": "Soumya",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.0.0",
    "eslint-config-standard": "^14.1.1",
    "eslint-plugin-import": "^2.20.2",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "firebase-admin": "^8.12.1"
  }
}
